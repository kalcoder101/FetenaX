# FetenaX Environment Configuration
# Copy this file to .env and fill in your values
# For InfinityFree hosting, the defaults in db.php already work

# Application
APP_ENV=development              # development | production
ALLOW_SIGNUP=true                # true | false (set to false in production)

# Database
DB_HOST=sql208.infinityfree.com  # MySQL host
DB_PORT=3306                     # MySQL port (3306 is standard)
DB_USER=if0_42279707             # MySQL username
DB_PASS=nu2jDmHJhjhk             # MySQL password
DB_NAME=if0_42279707_fetenax_db  # MySQL database name

# Migration (set to false in production to skip schema/seed on every request)
DB_AUTO_MIGRATE=true             # true | false
